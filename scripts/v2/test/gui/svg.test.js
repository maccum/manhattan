//var assert = require('chai').assert;

/*describe('SVG DOM Tests', function () {
    var editSVGOnPage = require('../../src/gui/svg.js');

    var plot, layer;

    beforeEach(function() {
        plot = page().element('svg').attribute('id', 'plot').place(document.body);
        layer = page().element('g').attribute('id', 'layer-2').place(plot);
        editSVGOnPage.setLayer(2);
    });

    afterEach(function() {
        plot.remove();
    });

    describe('Transformation List', function () {
        it('initializes transformation list if empty', function () {
            //assert.equal(true, schema.xy({x: 0, y: 0}));
            var transformations = editSVGOnPage.transformations();
            console.log(transformations);
        });
    });
});*/