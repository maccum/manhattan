var plot = {
    zoomToGrid: {},
    currentZoomLayer: 0,
    display: function(zoom) {
        // display zoom layer, hide others
    },
    scale: function(scaleX, scaleY) {
        // change scale of current zoom layer
    },
    position: function(x, y) {
        // change position of zoom layer
    },
}