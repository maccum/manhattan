// interact with svg elements
/*class Element {
    constructor(element, svg) {
        this.element = element;
    }

    getTransform() {

    }

    addTransform() {
        var transforms = this.element.transform.baseVal;
        if (transforms.length ===  0 || transforms.getItem(0).type !== SVGTransform.SVG_TRANSFORM_TRANSLATE) {
            console.log("adding first transform");
            var translate = this.svg.createSVGTransform();
            translate.setTranslate(0, 0);
            this.container.transform.baseVal.insertItemBefore(translate, 0);
        }
    
        var transform = transforms.getItem(0);
        return transform;
    }

    addFirstTransform() {
    }


}

function moveGroup(selector, x, y) {

}

function scaleGroup(selector, scale) {

}

function hideGroup(selector) {
    $(id).hide();
}

function showGroup(selector) {
    $(id).show();
}*/